# Offline Smart HUD 사용 설명서

인터넷 없이 동작하는 데스크톱 일정 관리 + 로컬 AI 도우미입니다.
모든 데이터는 이 PC 안에만 저장됩니다.

---

## 1. 시작하기

| 실행 방법 | 명령 |
|---|---|
| 소스에서 실행 | `py main.py` |
| 자세한 로그와 함께 | `py main.py --debug` |
| 설정이 꼬였을 때 초기화 | `py main.py --reset-config` |
| 화면이 안 보일 때 (불투명 강제) | `py main.py --safe` |
| 빌드된 앱 | `OfflineSmartHUD.exe` |

처음 실행하면 마우스 커서가 있는 모니터의 **우측 상단**에 나타납니다.

---

## 2. 화면 구성

```
┌────────────────────────────────┐
│ ● 14:06:02      ▴  ⚙  —  ✕    │  ← 제목 표시줄 (드래그해서 이동)
├────────────────────────────────┤
│  일정      AI                  │  ← 탭
│ ┌────────────────────────┐ ▤ ＋│  ← 자연어 입력창
│ 열린 일정 5    다음 회의 · 46분 후 │  ← 요약
│ ○ 주간 팀 회의        [매주 (월)]│
│   오늘 14:52   46분 1초 후      │
└────────────────────────────────┘
```

- **● 상태 점** : 초록 준비 완료 · 노랑 로딩 중 · 회색 모델 없음 · 빨강 오류
- **▴** 접기/펼치기 (제목 표시줄 더블클릭도 동일)
- **⚙** 설정 · **—** 트레이로 숨기기 · **✕** 트레이로 이동(종료 아님)
- 완전히 종료하려면 마우스 **우클릭 → 종료** 또는 트레이 아이콘 우클릭 → 종료

### 평소에는 흐릿하게, 쓸 때는 선명하게
마우스를 올리지 않으면 창이 흐려져 배경에 묻힙니다. 마우스를 올리면 즉시
선명해집니다. **글자는 항상 불투명하게 유지**되고 배경만 투명해지므로 글씨가
사라지는 일은 없습니다. 진하기는 `설정 → 모양 → 투명도`에서 조절합니다.

---

## 3. 일정 등록 (핵심 기능)

입력창에 **평소 말하듯이** 적고 Enter를 누르세요. 날짜·시간이 들어 있으면
**AI를 거치지 않고 즉시** 등록됩니다. (오프라인 규칙 기반 분석기라 항상 같은
결과가 나오고, 시간이 엉뚱하게 잡히는 일이 없습니다.)

### 바로 등록되는 표현들

| 입력 예시 | 결과 |
|---|---|
| `내일 오전 10시 돌스냅 촬영` | 내일 10:00 · 돌스냅 촬영 |
| `모레 오후 3시 미팅` | 모레 15:00 |
| `30분 뒤 휴식` | 30분 후 |
| `2시간 뒤 회의실 예약` | 2시간 후 |
| `8월 15일 14:30 팀 미팅` | 8/15 14:30 |
| `1pm on August 27th JB BODA 미팅` | 8/27 13:00 · JB BODA 미팅 |
| `Aug 27 2pm broker call` | 8/27 14:00 |
| `27 August 3pm renewal meeting` | 8/27 15:00 |
| `8/24 경영전략 엑셀 제출` | 8/24 09:00 |
| `3영업일 뒤 서류 제출` | 주말·공휴일 건너뜀 |
| `다음 영업일 결재 확인` | 다음 근무일 |
| `다음주 수요일 2시 반 치과` | 다음 수요일 14:30 |
| `낼모레 점심때 김부장님 미팅` | 모레 12:00 |
| `담달 첫째주 금요일 회식` | 다음달 첫 금요일 |
| `8월 말일 오후 6시 마감` | 8/31 18:00 |
| `이번주 주말 브런치` | 다가오는 토요일 |
| `tomorrow 3pm dentist` | 내일 15:00 |

`8/24` 처럼 슬래시로 쓴 날짜도 됩니다. 단 `3/4 정도만 끝냈어` 같은 분수는
날짜로 보지 않습니다.

### 한 번에 여러 건 등록하기

각 항목에 **날짜가 하나씩 붙어 있으면** 한 문장으로 여러 건을 넣을 수 있습니다.
구분자는 ` / `, `,`, `그리고`, 줄바꿈입니다.

> `8/24 경영전략 엑셀 제출 / 8/31 ppt 1차 제출 등록`
>
> → ✅ 일정 2건 등록 완료
> 1. '경영전략 엑셀 제출' — 08/24 09:00
> 2. 'ppt 1차 제출' — 08/31 09:00

날짜가 없는 항목이 하나라도 섞여 있으면 **나누지 않고 한 건으로 등록합니다.**
`8/24 엑셀 제출, 그리고 나중에 ppt도` 처럼 쓰면 "나중에 ppt도"까지 제목에
그대로 들어갑니다. 못 알아들은 부분을 조용히 버리는 것보다, 눈에 보이게
남겨두는 편이 안전하기 때문입니다. 이럴 땐 날짜를 붙여 다시 말해주세요.

### 영업일로 말하기

`3영업일 뒤`, `다음 영업일` 을 알아듣습니다. 주말과 공휴일을 건너뛰므로
금요일에 `3영업일 뒤` 라고 하면 다음 주 **수요일**입니다. (`3일 뒤` 는 월요일)

**양력 공휴일과 대체공휴일은 자동으로 계산됩니다.** 신정·삼일절·어린이날·
현충일·광복절·개천절·한글날·성탄절, 그리고 주말에 겹쳤을 때 생기는 대체공휴일은
법으로 정해진 규칙이라 연도 제한 없이 맞습니다.

**음력 공휴일(설날·추석·부처님오신날)만 표에 들어 있습니다.** 음력은 날짜에서
계산할 수 없기 때문입니다. **2030년까지** 수록돼 있고 그중 2027년까지는 검증된
값입니다. 그 이후, 그리고 회사 창립기념일 같은 자체 휴무일은 데이터 폴더의
**`holidays.txt`** 에 적어주세요.

```
2026-12-24  창립기념일
-2026-10-09          ← 맨 앞 '-' 는 기본 공휴일을 취소
```

고친 뒤에는 앱을 다시 켜야 반영됩니다. 현재 상태는 **설정 > 동작 > 근무 시간**
아래에 표시됩니다.

### 영어 입력도 됩니다

`August 27th`, `Aug 27`, `27 August`, `Dec 1, 2026` 형식과 `1pm`, `at 10am` 을
알아듣습니다. 해외 브로커 메일에서 한 줄 복사해 붙여넣어도 됩니다.

> `1pm on August 27th   JB BODA 미팅`
> → 8/27 13:00 · **JB BODA 미팅**

날짜를 떼어내고 남은 `on`, `at` 같은 전치사는 제목에서 정리하지만,
`hands on training` 처럼 제목 안에 들어 있는 것은 건드리지 않습니다.

### 반복 일정

| 입력 예시 | 반복 |
|---|---|
| `매일 아침 7시 운동` | 매일 |
| `매주 월요일 10시 회의` | 매주 (월) |
| `매주 화요일이랑 목요일 7시 헬스` | 매주 (화,목) |
| `매주 월수금 8시 스터디` | 매주 (월,수,금) |
| `매월 25일 월급 확인` | 매월 (25일) |

> **31일 반복**은 2월에 28일로 자동 조정되고, 3월에 다시 31일로 돌아옵니다.

### 요일 한 글자는 문맥이 있어야 요일로 봅니다
`수`, `일`, `목`, `금`은 평범한 우리말 단어이기도 합니다 (`할 수 있다`, `일 처리`,
`목이 아파`, `지금`). 그래서 한 글자는 아래 경우에만 요일로 인식합니다.

| 인식됨 | 인식 안 됨 |
|---|---|
| `수요일 10시 회의` (온전한 형태) | `너가 할 수 있는게 뭐야?` |
| `매주 수 10시 스터디` (매주 뒤) | `볼 수 있어?` |
| `다음주 수 3시 외근` (다음주 뒤) | `일 처리 좀 도와줘` |
| `매주 화,목 7시` (나열) | `목이 아파` |
| `수 10시` (바로 뒤에 시각) | `수가 없네` |

또한 **물음표로 끝나는 문장은 일정으로 등록하지 않습니다.** `내일 시간 될까?`는
질문이므로 대화로 처리됩니다. 질문형이라도 등록하고 싶으면 `추가해줘`를 붙이세요.

### 시간 해석 규칙 두 가지
- **오전/오후를 안 쓴 1~6시는 오후**로 봅니다. `2시 반 치과` → 14:30
  (오전이 필요하면 `오전 2시 반`이라고 쓰세요.)
- 날짜 없이 시간만 쓰고 그 시각이 지났으면 **다음 날**로 잡힙니다.

### 시간을 못 찾았을 때
`점심 뭐 먹지`처럼 애매하면 **확인 창**이 뜹니다. 날짜·시간을 직접 고르고
저장하면 됩니다. 이때만 AI가 보조로 해석을 시도합니다(그 결과도 반드시
확인 창을 거칩니다).

`▤` 버튼이나 `Ctrl+N`으로 처음부터 직접 입력할 수도 있습니다.

### 목록 필터
입력창 아래 알약 버튼으로 목록을 좁힐 수 있습니다.

| 버튼 | 내용 |
|---|---|
| `전체` | 모든 일정 |
| `오늘` | 오늘 안에 예정된 일정 + 아직 처리 못 한 지난 일정 |
| `반복 ↻` | 매일·매주·매월 반복 업무만 |

선택은 다음 실행에도 유지되고, 옆에 `표시개수/전체개수`가 나옵니다.
요약 줄과 지난 일정 배지는 **필터와 무관하게 항상 전체 기준**입니다 —
필터를 걸었다고 밀린 일이 안 보이면 안 되니까요.

### 일정 다루기
- **○** 원을 누르면 완료 처리 (다시 누르면 취소)
- 항목에 마우스를 올리면 **✎ 수정 / ✕ 삭제** 버튼이 나타납니다
- 남은 시간은 1시간 이내면 주황색, 지난 일정은 빨간색으로 표시됩니다
- 제목 앞의 **↻** 표시는 반복 일정이라는 뜻입니다 (일회성 일정에는 없습니다)

### 반복 일정을 완료하면 — 다음 주기로 이월
반복 일정은 "영원히 끝나는" 일이 아니라 매번 돌아오는 할 일입니다. 그래서
체크하면 사라지지 않고 **다음 주기로 넘어갑니다.**

```
매월 12일 특약OS이월  (08/12)   ← 체크
      ↓
특약OS이월 완료! 다음 일정: 09/12 09:00 이월됨   ← 토스트 안내
      ↓
매월 12일 특약OS이월  (09/12)   ← 목록에 그대로, 다음 날짜로
```

- 예정일보다 **미리** 체크해도 한 주기가 정상적으로 넘어갑니다
- 오래 밀린 반복 일정은 지난 날짜가 아니라 **다가오는 날짜**로 맞춰집니다
- 일회성 일정은 기존처럼 완료 표시(취소선)로 남습니다
- 알림 카드의 **완료** 버튼도 같은 규칙으로 동작합니다

> **알림을 받고 누른 완료는 한 주기만 넘어갑니다.** 알람이 울리는 순간 이미
> 다음 주기로 이월되기 때문에, 그 카드의 완료가 또 이월시키면 한 달이 통째로
> 사라집니다. (8/12 알림 → 완료 → 9월이 없어지고 10/12가 되는 문제였고,
> 지금은 9/12가 유지됩니다.)

### 완료한 일정은 하루 뒤 정리됩니다
체크한 1회성 일정은 그날 하루는 목록에 남아 있다가 **24시간 뒤 사라집니다.**
오늘 뭘 했는지는 보여야 하지만, 목록이 계속 쌓이면 한눈에 안 들어오니까요.
**업무 보고용 완료 기록은 지워지지 않습니다** — 정리해도 주간보고에는 그대로
나옵니다. 시간을 바꾸거나 끄려면 `설정 → 동작 → 완료 항목 자동 정리`.

---

## 4. 알림

시간이 되면 소리가 나고, 테두리가 하늘색으로 빛나며 알림 카드가 뜹니다.

- **완료** : 일정을 완료 처리 (반복 일정이면 다음 주기로 이월)
- **5분 / 15분 / 1시간** : 그만큼 미루기
  (반복 일정은 원래 주기를 건드리지 않고 1회용 알림만 따로 추가합니다)
- 창이 숨겨져 있어도 알림이 오면 자동으로 나타납니다
- 노트북을 오래 닫아두었다면, 놓친 알림은 최대 5개만 울리고 나머지는 조용히 다음 시각으로 넘어갑니다

### 놓친 알림 다시 알려주기

알림 카드가 떴는데 아무것도 누르지 않고 지나가면 (자리를 비웠거나 다른 일을
하다가 그냥 사라진 경우) **10분 뒤에 한 번 더** 알려줍니다. 기본 3회까지
반복하고 멈춥니다.

다시 뜨는 카드는 평소 하늘색이 아니라 **노란색**이고, 위쪽에
`★ 놓친 알림 · 1번째` 라고 표시됩니다. 그 아래에 원래 예정 시각과 얼마나
지났는지가 함께 나옵니다.

> 예: `08/19 14:00 예정 · 1시간 10분 지남 · 매월 (19일)`

반복 일정이라도 **놓친 그 시각**을 보여줍니다. (일정 자체는 이미 다음 주기로
넘어가 있지만, 지금 알려줘야 하는 건 놓친 쪽이니까요.)

**다시 알리지 않게 하려면** — 셋 중 아무거나 하면 됩니다.

| 동작 | 재알림 |
|---|---|
| **완료** 를 누른다 | 멈춤 |
| **5분 / 15분 / 1시간** 미루기를 누른다 | 멈춤 (미룬 시각에 다시 울림) |
| 카드의 **✕** 를 눌러 직접 닫는다 | 멈춤 (봤다는 뜻이므로) |
| 아무것도 안 하고 카드가 저절로 사라진다 | **계속** — 이게 '놓친' 경우입니다 |

밀린 게 여러 개여도 한 번에 몰아서 띄우지 않고 **1분에 하나씩**, 오래된
것부터 차례로 알려줍니다. 앱을 껐다 켜도 이어집니다.

끄거나 간격·횟수를 바꾸려면 **설정 > 동작** 에서 조절하세요.

---

## 4-2. 업무 보고 만들기

금요일마다 쓰는 그 보고서입니다. **우클릭 → 업무 보고 만들기** 를 누르면
완료 처리한 항목이 기간별로 정리되어 나옵니다.

- **이번 주 / 지난 주 / 이번 달** 중에 선택
- `예정 항목도 포함` 을 켜면 아직 안 끝난 일도 아래에 붙습니다
- 내용을 고친 뒤 **복사** 를 눌러 보고서에 붙여넣으세요

```
[08/17 ~ 08/23] 완료한 업무
- 경영전략 엑셀 제출  (08/20(목))
- 특약OS이월 ↻  (08/19(수))

[예정 · 진행 중]
- ppt 1차 제출  (08/31(월))
```

**반복 일정도 기록됩니다.** 반복 일정은 완료하면 다음 주기로 넘어가버려서
목록만 봐서는 이번 달에 했는지 알 수 없는데, 완료한 시점을 따로 적어두기
때문입니다. `↻` 표시가 반복 업무입니다.

AI 탭에서 `이번주 한 일`, `지난주 한 일`, `주간보고` 라고 해도 됩니다.
**이 내용은 전부 DB에서 나옵니다 — AI가 문장을 만들지 않으므로 없는 일이
적힐 수 없습니다.**

> 참고: `매주 월요일 9시 주간보고` 처럼 시간이나 반복이 붙어 있으면
> 보고서 요청이 아니라 일정 등록으로 처리합니다.

---

## 4-3. 근무 시간에만 알리기

**설정 > 동작 > 근무 시간** 에서 켜면, 근무 시간 밖에는 알림이 울리지
않습니다. 기본은 꺼져 있습니다.

- 근무 시간 (기본 09:00 ~ 18:00), 점심시간 제외(선택), 주말·공휴일 제외
- **알림이 사라지지 않습니다.** 밤 10시에 예정된 알림은 다음 근무 시간이
  시작될 때 울립니다. 그 사이에 PC를 꺼도 마찬가지입니다.

## 4-4. Outlook 오늘 일정

Outlook 캘린더에 오늘 잡힌 일정이 있으면 목록 위에 한 줄로 보여줍니다.

> `▤ 14:00  3분기 요율 검토 회의 · 소회의실  (+2)`

마우스를 올리면 오늘 전체가 보입니다. **읽기만 하고 아무것도 바꾸지 않으며**,
회의 자체는 Outlook에 그대로 있습니다(HUD로 복사하지 않습니다). 제목·시간·장소만
읽고 주최자나 참석자 정보는 건드리지 않습니다 — 그 항목들이 Outlook 보안 경고를
띄우는 대상이기 때문입니다. 끄려면 **설정 > 동작 > Outlook 오늘 일정 표시**.

---

## 5. AI 어시스턴트 (AI 탭)

### 채팅으로 일정 조작하기 (명령 인식)
채팅창에 **지시**를 적으면 AI에게 물어보는 대신 앱이 바로 실행합니다.
이 판단은 규칙 기반이라 즉시 처리되고, 모델이 엉뚱한 답을 지어낼 여지가 없습니다.

| 입력 | 동작 | 응답 |
|---|---|---|
| `매월 12일 특약OS이월 추가해줘` | 일정 등록 | `✔ '특약OS이월' 일정이 08/12 09:00 (매월 (12일)) 으로 등록되었습니다.` |
| `내일 오전 10시 촬영 등록해줘` | 일정 등록 | `✔ …` |
| `오늘 일정 알려줘` | 목록 조회 | `☰ 오늘 일정 2건` + 목록 |
| `이번주 할일 보여줘` | 이번 주 조회 | `☰ 이번 주 일정 …` |
| `완료된 일정 정리해줘` | 완료 항목 삭제 | `✔ 완료된 일정 2건을 정리했습니다.` |
| `치과 예약 삭제해줘` | 해당 일정 삭제 | `✕ '치과 예약' 일정을 삭제했습니다.` |
| `내일 오전 10시 회의` | **동사 없이도** 일정 등록 | `✔ '회의' 일정이 …으로 등록되었습니다.` |
| `특약 메일 알림 삭제해줘` | 메일 감지 규칙 삭제 | `✕ '특약OS이월' 메일 감지 알림 규칙이 삭제되었습니다.` |

> **"추가해줘"를 안 붙여도 됩니다.** `내일 오전 10시 회의`처럼 날짜·시간이
> 들어 있으면 그대로 일정으로 등록됩니다. 반대로 `오늘 일정 알려줘`처럼
> 물어보는 문장은 조회로 처리되니 헷갈릴 일은 없습니다.

- 조회 범위는 `오늘 / 내일 / 이번주 / 이번달 / 전체 / 완료`를 인식합니다
- 같은 이름의 일정이 여러 개면 삭제하지 않고 후보를 알려줍니다
- `회의 언제가 좋을까?` 처럼 **질문**은 명령으로 보지 않고 평소대로 대화합니다
- 명령을 처리한 결과는 일정 탭에도 즉시 반영됩니다

### "등록했다"는데 목록에 없을 때

실제로 등록되면 답변이 **✅ 로 시작**하고, 일정 탭에 **즉시** 나타나며,
화면 아래에 `○○ 등록됨` 토스트가 뜹니다. 셋이 같이 움직입니다.

AI가 등록하지도 않고 "등록 완료"라고 답하는 경우가 있었습니다. 명령으로
인식되지 않은 문장이 일반 대화로 넘어가면, 모델이 그럴듯한 확인 문구를
지어내는 것입니다. 지금은 이런 답변을 감지해서 아래 안내로 **바꿔서**
보여줍니다.

> ⚠ 방금 답변에 '등록했다'는 내용이 있었지만 실제로 저장되지 않았습니다.

이 안내가 보이면 날짜를 붙여 다시 말하거나, 위쪽 입력창을 쓰세요.
**일정 탭에 실제로 보이는 것만 등록된 것입니다.**

### 일반 대화
로컬 GGUF 모델로 동작합니다. 질문, 메일 초안, 메모 요약 등에 쓸 수 있습니다.

- Enter 전송 · Shift+Enter 줄바꿈
- 생성 중에는 **■** 버튼으로 즉시 중단
- 모델이 생각하는 동안 `◌ 생각하는 중…`이 표시되고, 생각 과정은 감춘 뒤 답변만 보여줍니다

### 모델 넣기 / 바꾸기

**데이터 폴더**의 `models` 안에 `.gguf` 파일을 넣습니다.

```
%LOCALAPPDATA%\OfflineSmartHUD\models\
```

탐색기 주소창에 위 경로를 그대로 붙여넣으면 열립니다 (`AppData`는 숨김
폴더라 눈으로는 찾을 수 없습니다). 현재 경로는 `설정 → AI`에도 표시됩니다.

**왜 프로그램 폴더가 아닌가** — 업그레이드는 프로그램 폴더를 통째로
덮어쓰는 방식입니다. 모델을 거기 두면 1GB가 넘는 파일을 매번 다시 넣어야
합니다. 데이터 폴더에 두면 새 버전으로 덮어써도 그대로 남습니다.

여러 개가 있으면 **가장 최근에 넣은 파일**이 선택되고, 채팅 탭 아래에 선택된
파일 이름이 표시됩니다. 특정 모델로 고정하려면 `설정 → AI → GGUF 파일 선택`.

> 프로그램 폴더에 있는 모델도 계속 인식은 됩니다(기존 설치 호환). 다만 그
> 경우 시작할 때 **"업그레이드 시 사라집니다"** 라고 경고하고 옮길 위치를
> 함께 알려줍니다. 두 곳에 다 있으면 **데이터 폴더 쪽이 우선**입니다.

### 모델 크기와 속도 (이 PC 기준, CPU)

| 모델 | 크기 | 응답 체감 |
|---|---|---|
| Qwen3-1.7B | 1.3 GB | 빠름 (~9 tok/s) |
| Qwen3.5-2B | 1.4 GB | 보통 |
| Qwen3.8-4B | 2.6 GB | 느림 (~3~4 tok/s · 답변당 20~40초) |
| Qwen3.5-4B | 3.0 GB | 매우 느림 (분 단위) |

**4B는 글은 훨씬 잘 쓰지만 2배 넘게 느립니다.** 스레드를 늘려도 거의 안
빨라집니다(2→8 스레드에 24% 개선). 메모리 대역폭이 병목이라 코어를 더 준다고
해결되지 않습니다. 채팅을 자주 쓰면 1.7B, 메일 초안처럼 가끔 제대로 된 문장이
필요하면 4B가 낫습니다.

2 GB 이상 모델을 고르면 느릴 수 있다는 안내가 한 번 표시됩니다.

### ⚠ 작은 모델의 한계 — 꼭 알아두세요
1~4B급 모델은 **모르는 고유명사를 그럴듯하게 지어냅니다.** 예를 들어 "돌스냅"을
물으면 실제 뜻(돌잔치 기념 촬영)이 아니라 엉뚱한 영어 제품명을 만들어낼 수
있습니다. 사실 확인이 필요한 내용은 신뢰하지 마세요. 문장 다듬기, 초안 작성,
요약처럼 **입력한 내용을 가공하는 작업**에 쓰는 것이 안전합니다.

> 일정 등록은 이 문제와 무관합니다. 날짜·시간이 있는 입력은 AI를 거치지
> 않고 규칙 기반으로만 처리됩니다.

### 대화가 이상해지면 — 기록 지우기
지난 답변이 다음 답변의 참고 자료로 들어가기 때문에, 한 번 이상한 답이 나오면
계속 영향을 줍니다. 이럴 때 기록을 지우세요.

- `Ctrl+Shift+C`
- 우클릭 메뉴 → 대화 기록 지우기
- 트레이 아이콘 우클릭 → 대화 기록 지우기

시작할 때 빈 메시지나 깨진 기록은 자동으로 정리됩니다.

---

## 6. 기다리는 메일 알림 (Outlook 연동)

"그 메일 오면 알려주고, 뭘 해야 하는지도 같이 띄워줘" 를 위한 기능입니다.
설치된 Outlook의 받은 편지함을 10초마다 확인합니다.

### 등록하기
AI 탭에 이렇게 적으면 됩니다.

```
'특약OS이월' 메일 오면 '결재 시스템 승인' 리마인드해줘
월마감 이메일 오면 마감 자료 취합
계약서 관련 메일 받으면 스캔본 저장해줘
```

→ `★ '특약OS이월' 메일 수신 감지 알림이 등록되었습니다.`
　 `● 후속 업무: 결재 시스템 승인`

### 확인하기
`기다리는 메일 목록` · `메일 알림 리스트` · `메일 리마인더`

```
☰ 메일 감지 알림 2건 (대기 중 1건)
● 특약OS이월
    → 결재 시스템 승인
✔ 월마감 (수신 완료)
    → 자료 취합
```

### 메일이 도착하면
HUD가 저절로 나타나 100% 불투명해지고, 테두리가 빛나고 알림음이 울리며
카드가 뜹니다.

```
★ [기다리던 메일 도착!]
제목: [공지] 특약OS이월 처리 안내
발신자: 김팀장
┌──────────────────────────┐
│ ● [후속 진행 업무]         │
│ 1. 결재 시스템 승인        │
│ 2. 담당자 이메일 공유      │
└──────────────────────────┘
                [확인 / 닫기]
```

### 동작 규칙
- **제목과 본문** 둘 다 검사하며, 키워드는 대소문자를 구분하지 않습니다
- 키워드를 쉼표로 여러 개 지정하면 그중 하나만 맞아도 알립니다
- 한 번 알린 규칙은 자동으로 "수신 완료" 처리되어 반복해서 울리지 않습니다
- **등록 시점 이후에 도착한 메일만** 대상입니다 (이미 와 있던 메일은 울리지 않음)
- 등록된 규칙이 하나도 없으면 Outlook에 접속하지 않습니다

### Outlook이 꺼져 있으면
- 규칙 등록·조회는 그대로 됩니다. 감시만 대기 상태가 됩니다
- 상태가 `Outlook 미실행 (열면 자동 연결됩니다)` 로 표시되고, Outlook을 켜면
  자동으로 연결됩니다
- **앱이 Outlook을 대신 실행하지는 않습니다.** 백그라운드 프로그램이 마음대로
  Outlook을 띄우지 않도록 "실행 중일 때만 연결" 방식을 씁니다
- Outlook이 설치되어 있지 않거나 pywin32가 없으면 그 사실을 알려주고, 나머지
  기능은 평소대로 동작합니다

---

## 7. 설정 (⚙ 또는 Ctrl+,)

변경하면 **즉시 반영**됩니다. 취소하면 열었을 때 상태로 되돌아갑니다.

**모양** — 테마(다크/미드나잇/슬레이트/라이트/고대비), 강조색(직접 지정 가능),
글자 크기(80~140%), 간격(좁게/보통/넓게), 모서리 둥글기, 투명도 3종,
초 표시·요약·남은 시간·그림자 켜기/끄기

**동작** — 항상 위에 표시, 위치 잠금, 위치 기억, 시작 시 트레이로, 기본 위치,
알림음과 음량, 트레이 풍선, 테두리 반짝임, 알림 유지 시간, 미루기 분,
**놓친 알림 다시 알리기(켜기·간격·횟수)**, 확인 주기, 완료 일정 숨기기,
삭제 전 확인

**AI** — 모델 파일 선택, 사고 과정(끄기/숨기기/표시), 응답 길이, 창의성,
기억할 대화 수, 컨텍스트 크기, 스레드 수, 미리 로드, 일정 분석에 AI 사용

**정보** — 버전과 모델 정보, 이 설명서 열기, 폴더/로그 열기, 단축키 목록

설정은 `config.json`에 사람이 읽을 수 있는 형태로 저장됩니다. 직접 편집해도
되고, 값이 잘못되면 자동으로 안전한 기본값으로 보정됩니다.

---

## 8. 단축키

| 키 | 기능 |
|---|---|
| `Enter` | 일정 추가 / 메시지 전송 |
| `Shift+Enter` | 줄바꿈 |
| `Ctrl+N` | 일정 직접 입력 창 |
| `Ctrl+,` | 설정 |
| `Ctrl+Shift+C` | 대화 기록 지우기 |
| `Ctrl+Tab` | 탭 전환 |
| `Ctrl+L` | 입력창으로 포커스 |
| `Esc` | 트레이로 숨기기 |
| `Ctrl+드래그` | 제목 표시줄 밖에서도 창 이동 |
| 제목 표시줄 더블클릭 | 접기 / 펼치기 |

---

## 9. 문제가 생겼을 때

앱은 **아무 설명 없이 사라지지 않도록** 만들어져 있습니다. 오류가 나면 이유를
보여주고 계속 실행되며, `logs` 폴더에 기록을 남깁니다.

| 증상 | 해결 |
|---|---|
| 창이 안 보인다 | 흐린 상태일 수 있습니다. 트레이 아이콘 클릭, 또는 `py main.py --safe` |
| 화면 밖으로 나갔다 | 우클릭 → 위치 → 우측 상단 |
| 설정을 바꾸다 망가졌다 | `py main.py --reset-config` |
| 채팅이 비활성 | `models` 폴더에 `.gguf` 파일이 있는지 확인 (일정 기능은 정상 동작) |
| 답변이 너무 느리다 | 더 작은 모델 선택, 또는 `설정 → AI → 사고 과정: 끄기`, 응답 길이 줄이기 |
| 답변이 이상하다 | `Ctrl+Shift+C`로 기록 지우기 |
| 시작 직후 잠깐 멈춘다 | 모델을 읽는 중입니다. `설정 → AI → 미리 로드`를 끄면 첫 메시지 때 불러옵니다 |
| "이미 실행 중입니다" | 트레이 확인. 비정상 종료했다면 `OfflineSmartHUD.lock` 삭제 |
| 메일 알림이 안 온다 | Outlook 실행 여부 확인. `기다리는 메일 목록`으로 상태 확인 |
| "Outlook 미실행" 표시 | Outlook을 켜면 자동 연결됩니다 (앱이 대신 켜지 않음) |
| 알람이 안 울린다 | `설정 → 동작 → 알림음` 확인, `logs\hud.log` 확인 |

### 백업에서 되돌리기
`설정 → 정보 → 백업에서 복원…`

앱은 실행할 때마다 자동으로 백업을 남기고 최근 10일치를 보관합니다. 목록에는
날짜와 함께 **그 백업에 일정이 몇 건 들어있는지**가 표시되므로, 마지막으로
멀쩡했던 시점을 바로 고를 수 있습니다.

- 복원하면 현재 내용은 `backups
eplaced-…` 폴더에 따로 보관됩니다.
  잘못 골라도 되돌릴 수 있습니다.
- 복원 후에는 **앱을 다시 시작해야** 합니다 (자동으로 종료됩니다).
- 대화 기록은 함께 복원할지 선택할 수 있습니다.

### 기록 파일
```
logs\hud.log              일반 실행 기록 (최대 4개 순환)
logs\crash_*.txt          오류 상세 + 환경 정보
logs\faulthandler.log     프로그램이 강제 종료된 경우의 흔적
```
문의할 때 `logs` 폴더를 함께 보내주시면 원인 파악이 빠릅니다.

---

## 10. 새 버전으로 업그레이드

**프로그램 폴더와 데이터 폴더가 분리되어 있습니다.
그래서 프로그램 폴더는 통째로 덮어써도 됩니다.**

```
C:\FTC_downloads\OfflineSmartHUD\        ← 프로그램. 통째로 교체 OK
    OfflineSmartHUD.exe
    _internal\
    datadir.txt                          ← 데이터 위치를 적어두는 파일(선택)

%LOCALAPPDATA%\OfflineSmartHUD\          ← 데이터. 절대 안 지워짐
    schedules.db          일정 + 메일 감지 규칙
    chat_history.db       대화 기록
    config.json           설정
    logs\                 로그
    backups\<날짜>\       DB 자동 스냅샷 (최근 10일)
    models\               AI 모델 (.gguf) - 여기 두면 업그레이드해도 안 지워짐
```

`%LOCALAPPDATA%` 는 보통 `C:\Users\<사번>\AppData\Local` 입니다.
탐색기 주소창에 `%LOCALAPPDATA%\OfflineSmartHUD` 를 붙여넣으면 바로 열립니다.

### 업그레이드 방법
1. 기존 `OfflineSmartHUD` **폴더를 지우거나** 새 폴더로 덮어씁니다
2. 실행합니다

끝입니다. 일정·메일 규칙·설정·로그는 데이터 폴더에 있어서 영향받지 않습니다.

### 기존 설치본에서 처음 올릴 때
예전 버전은 데이터를 exe 옆에 뒀습니다. 새 버전을 처음 실행하면 그 데이터를
데이터 폴더로 **자동 복사**하고, 옛 폴더에 `DATA_MOVED.txt` 를 남깁니다.
원본은 지우지 않으니 정상 동작을 확인한 뒤 예전 `.db` 파일을 삭제하세요.

> 그러니 **처음 한 번은** 옛 폴더를 지우기 전에 새 exe 를 한 번 실행해 주세요.
> (이미 지웠다면 옛 `schedules.db` 를 데이터 폴더에 넣어주면 됩니다.)

### 데이터 폴더를 다른 곳에 두고 싶다면
`OfflineSmartHUD.exe` 옆에 `datadir.txt` 를 만들고 경로 한 줄만 적으세요.

```
D:\내문서\OfflineSmartHUD_Data
```

명령행이 되는 환경이라면 `--data-dir "경로"` 또는 환경변수
`OFFLINESMARTHUD_DATA` 도 같은 역할을 합니다.

### 그래도 폴더를 통째로 옮겨야 한다면 — 백업할 파일
데이터 폴더에서 이 3개만 챙기면 전부 복구됩니다.

| 파일 | 내용 |
|---|---|
| `schedules.db` | **일정 + 기다리는 메일 규칙** (제일 중요) |
| `config.json` | 설정 (테마·투명도·알림) |
| `chat_history.db` | 대화 기록 (없어도 무방) |

### 자동 백업
앱은 시작할 때마다 두 DB의 스냅샷을 `backups\<날짜>\` 에 남깁니다(최근 10일).
사고가 나도 여기서 `schedules.db` 를 되돌리면 복구됩니다.
`config.json` 의 `backup_on_start`, `backup_keep_days` 로 조절합니다.

### upgrade.ps1 (명령행이 되는 환경용)
데이터를 여전히 exe 옆에 두고 쓰는 경우를 위해 스크립트도 동봉되어 있습니다.
`.\upgrade.ps1 -Target "<설치폴더>"` — 폴더 분리를 쓰신다면 필요 없습니다.

---

## 11. 저장되는 파일

```
schedules.db      일정 + 기다리는 메일 규칙   ← 업그레이드 시 보존 필수
chat_history.db   대화 기록                  ← 보존
config.json       설정                       ← 보존
backups\<날짜>\   DB 자동 스냅샷 (최근 10일)  ← 보존
logs\             기록
models\           AI 모델 (.gguf)
assets\notify.wav 알림음 (자동 생성)
```

일정과 대화는 이 PC를 벗어나지 않습니다. 네트워크로 내보내는 코드는 앱 안에
존재하지 않습니다. 폴더 전체를 복사하면 설정과 데이터를 그대로 옮길 수 있습니다.

